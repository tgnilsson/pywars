# Default values for pywarschart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: tnilsson/pywars
  tag: latest
  pullPolicy: IfNotPresent
